require 'spec_helper'

describe Xctestrunner do
  it 'has a version number' do
    expect(Xctestrunner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
